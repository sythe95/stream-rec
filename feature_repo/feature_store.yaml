project: stream_rec
registry: data/registry.db
provider: local
entity_key_serialization_version: 3

online_store:
  type: redis
  connection_string: "${REDIS_HOST}:${REDIS_PORT}"

offline_store:
  type: file