global:
  scrape_interval: 5s # Scrape the API every 5 seconds for a real-time feel

scrape_configs:
  # This tells Prometheus to look at our FastAPI container
  - job_name: 'inference_api'
    static_configs:
      - targets: ['inference_api:8000']